"""Copyright 2025 Bundesdruckerei GmbH.

Licensed under Apache-2.0, see the accompanying file LICENSE

Main module of this project (remove/adjust as needed).
"""

import logging

from {{ package_name }} import __version__


def main() -> None:
    """Main function of this project.

    This is generated from the template, remove/adjust as needed.
    """
    logger.info(
        "generic info",
        extra={
            "version": __version__,
            "key": "value",
        },
    )


if __name__ == "__main__":
    from {{ package_name }}.loggingconfig import setup_logging

    # set the logging configuration
    setup_logging()
    logger = logging.getLogger(__name__)
    main()
